<% stylesheet_link_tag 'product.generic' %>
<div class="container-lg d-flex flex-row justify-content-center">

  <%= form_with(model: @store) do |form| %>
    <h1 class="mb-4">Store creation</h1>
    <% if @store.errors.any? %>
      <div style="color: red">
        <h2>
          <%= pluralize(@store.errors.count, 'error') %> prohibited this product from being saved:
        </h2>
        <ul>
          <% @store.errors.each do |error| %>
            <li>
              <%= error.full_message %>
            </li>
            </ul>
            </div>
          <% end %>
    <% end %>

    <div class="mb-3">
      <%= form.label :name, 'store name',  style: 'display: block' %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>

    <div>
      <%= form.submit class: 'btn btn-primary mt-5 w-50 position-relative top-100 start-50 translate-middle' %>
    </div>
  <% end %>
</div>