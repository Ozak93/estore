<nav class="navbar navbar-expand-lg bg-dark bg-dark">
  <div class="container-fluid text-bg-dark">
    <%= link_to 'E Store', user_home_path, class: "navbar-brand text-bg-dark" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon text-bg-dark"></span>
    </button>
    <div class="collapse navbar-collapse text-bg-dark" id="navbarNavDropdown">
      <ul class="navbar-nav text-bg-dark">
        <li class="nav-item">
          <%= link_to 'Home', user_home_path, class: 'nav-link active text-bg-dark', 'aria-current': 'page' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Categories', categories_path, class: 'nav-link text-bg-dark' %>
        </li>
        <li class="nav-item">
          <%= link_to 'Products', products_path, class: 'nav-link text-bg-dark' %>
        </li>
        <% if admin_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-bg-dark" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Users
            </a>
            <ul class="dropdown-menu">
              <li>
                <%= link_to "Store Owners", admins_users_store_owners_path, class: ' dropdown-item mb-1 text-center' %>
              </li>
              <li>
                <%= link_to "Customers", admins_users_customers_path, class: ' dropdown-item mb-1 text-center' %>
              </li>
              <li>
                <%= link_to "Show all users", admins_users_path, class: ' dropdown-item mb-1 text-center border-top' %>
              </li>
            </ul>
          </li>
        <% end %>

        <% if admin_signed_in? or owner_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-bg-dark" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Stores
            </a>
            <ul class="dropdown-menu">
              <% nav_stores.each do |store| %>
                <li>
                  <%= link_to store.name.capitalize, store_path(store), class: ' dropdown-item mb-1' %>
                </li>
              <% end %>
              <li>
                <%= link_to 'show all stores', stores_path, class: ' dropdown-item border-top text-center' %>
              </li>
            </ul>
          </li>
        <% end %>
        <li class="nav-item">
        </li>
      </ul>
    </div>
    <div class="pe-3">
      <% if user_signed_in? %>
        <%= link_to 'Sign out', destroy_user_session_path, data: { "turbo-method": :delete, turbo_confirm: "Are you sure?" }, class: 'nav-link' %>
      <% end %>
    </div>
  </div>
</nav>